server:
  port: 8080
#  tomcat:
#    threads:
#      max: 10
#      min-spare: 2

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  
    url: jdbc:mysql://localhost:3306/flash_link?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useUnicode=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
      timeout: 3000ms
      database: 15

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    personal.cx537.flashlink: DEBUG
